version: "3.9"

services:
  infinityx:
    # image: ghcr.io/infinity-x202/ddos-infinity-x:latest
    build: .
    container_name: ddos-infinity-x
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json
      - ./files:/app/files
